[package]
name = "coins-trading"
version = "0.1.0"
edition = "2024"

[dependencies]
pumpfun.workspace = true
jup-ag-sdk.workspace = true
coins-config = { path = "../config" }
coins-database = { path = "../database" }

[dev-dependencies]
coins-database = { path = "../database" }
